[project]
name = "voice-assistant"
version = "0.1.0"
description = "Voice assistant agent for NormaCore"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "openai",
    "SpeechRecognition",
    "PyAudio",
    "python-dotenv",
    "soundfile"
]

[tool.uv]
package = false
